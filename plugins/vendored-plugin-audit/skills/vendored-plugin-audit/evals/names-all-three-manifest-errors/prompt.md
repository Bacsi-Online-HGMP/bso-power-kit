---
description: All three plugin.json shape errors are named, each with its correct form.
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill]
---

`claude plugin validate` fails on plugins/pod-tools and the message only says "Invalid input". What is wrong with its plugin.json, and what should each broken line become?
